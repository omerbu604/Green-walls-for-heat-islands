# Private / sensitive data layers — never push to GitHub
data/private/

# Raw full-country OSM shapefiles (too large for GitHub — use clipped GeoJSON versions instead)
Layers/osm/*.shp
Layers/osm/*.dbf
Layers/osm/*.shx
Layers/osm/*.cpg
Layers/osm/*.prj

# Common data formats to keep local if needed
# *.gpkg

# Python
__pycache__/
*.pyc
.env
*.egg-info/

# Jupyter
.ipynb_checkpoints/

# OS
.DS_Store
Thumbs.db
